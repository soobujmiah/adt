{
  "build-tools": {
    "37.0.0": {"status": "verified", "aosp_tag": "platform-tools-35.0.2", "tested_on": "Fedora Asahi 43, GCC 15.2.1", "notes": "I built this from the same AOSP source as 35.0.2 (there's no platform-tools-37.0.0 tag in AOSP). I haven't published a release or local artifact for this version yet; install = source build."},
    "36.0.0": {"status": "verified", "aosp_tag": "platform-tools-35.0.2", "tested_on": "GitHub Actions ubuntu-24.04-arm (build); ARM64 PRoot Debian real-device fixture (binary execution)", "notes": "I confirmed via `git ls-remote --tags https://android.googlesource.com/platform/manifest` on 2026-09-01 that no platform-tools-36.0.0 tag exists in AOSP — the highest platform-tools-X.Y.Z tag is 35.0.2; Android 16/API 36 is only tagged as android-16.0.0_rN, a full-manifest release tag repos.json's individual component repos aren't guaranteed to carry. I built this from the same AOSP source as 35.0.2, matching the existing 37.0.0 precedent, via GitHub Actions (run 33532721357, workflow_dispatch on build.yml). CI's own ARM64 check passed, and I independently re-verified all 6 build-tools binaries as ARM64 ELF and actually executed them (aapt2, aapt, aidl, zipalign, dexdump, split-select) on my ARM64 PRoot Debian real-device fixture. I added this to fill the gap left by build-tools 36.0.0 as distributed by Google's sdkmanager for platforms;android-36, which is x86_64 and can't execute under my ARM64 PRoot (see docs/ANDROID_ARM64_BUILD_HANDOFF.md and the setup.sh doctor host-tool architecture check). Validated offline-installable artifact: artifacts/build-tools-36.0.0-linux-arm64.tar.gz (SHA256 in artifacts/SHA256SUMS). platform-tools 36.0.0 was also built as a side effect of the same AOSP source, but I intentionally didn't add it here — adb/fastboot are already covered by verified platform-tools 35.0.2 and there was no reason to duplicate that."},
    "35.0.2": {"status": "verified", "aosp_tag": "platform-tools-35.0.2", "tested_on": "Fedora Asahi 43, GCC 15.2.1; ARM64 PRoot Debian real-device fixture", "notes": "I installed and validated the native ARM64 aapt/aapt2/aidl/zipalign toolchain in my ARM64 PRoot environment. I validated the D8 8.2.33 JVM path and signed APK verification. I validated a real Android 16 arm64-v8a APK install, JNI loading, native execution, log output, and process survival. Validated offline-installable artifact: artifacts/build-tools-35.0.2-linux-arm64.tar.gz (SHA256 in artifacts/SHA256SUMS)."},
    "35.0.1": {"status": "unverified", "aosp_tag": "platform-tools-35.0.1", "notes": "I deliberately haven't built this — I confirmed the tag exists (git ls-remote --tags https://android.googlesource.com/platform/manifest, 2026-09-01), but it's an older patch of the same 35.0.x line as the already-verified, artifact-backed 35.0.2. build-tools is forward-compatible; I don't have a reason to prefer this older patch. I'll revisit only if a specific project needs an exact 35.0.1 pin."},
    "35.0.0": {"status": "unverified", "aosp_tag": "platform-tools-35.0.0", "notes": "STALE ENTRY: this aosp_tag doesn't exist. I confirmed via git ls-remote --tags https://android.googlesource.com/platform/manifest (2026-09-01) that the 35.0.x tags in AOSP are only 35.0.1 and 35.0.2 — there's no platform-tools-35.0.0. get_source.py would fail (or silently skip repos) if I built this as-is. It'd also be superseded in practice by verified 35.0.2 even if I found a correct tag — not planned."},
    "34.0.0": {"status": "unverified", "aosp_tag": "platform-tools-34.0.4", "notes": "I deliberately haven't built this — the tag exists, but no installed platform or known project of mine targets API 34 on this device (platforms;android-35 and android-36 are installed); build-tools is forward-compatible, so verified 35.0.2/36.0.0/37.0.0 already cover any API-34 target. I'll revisit only if a specific project requires an exact 34.x build-tools pin."}
  },
  "platform-tools": {
    "37.0.0": {"status": "verified", "aosp_tag": "platform-tools-35.0.2", "tested_on": "Fedora Asahi 43, GCC 15.2.1", "notes": "I built this from the same AOSP source as 35.0.2 (there's no platform-tools-37.0.0 tag in AOSP). I haven't published a release or local artifact for this version yet; install = source build."},
    "35.0.2": {"status": "verified", "aosp_tag": "platform-tools-35.0.2", "tested_on": "Fedora Asahi 43, GCC 15.2.1; ARM64 PRoot Debian real-device fixture", "notes": "adb and fastboot are installed and working. I used adb to install, launch, inspect, and collect logs from my physical Android 16 arm64-v8a device during native APK validation. Validated offline-installable artifact: artifacts/platform-tools-35.0.2-linux-arm64.tar.gz (SHA256 in artifacts/SHA256SUMS)."},
    "35.0.1": {"status": "unverified", "aosp_tag": "platform-tools-35.0.1", "notes": "I deliberately haven't built this — it's an older patch of the same line as verified 35.0.2; adb/fastboot are protocol-stable across patches, so I don't have a reason to prefer this version. I'll revisit only if a specific project needs an exact 35.0.1 pin."},
    "34.0.4": {"status": "unverified", "aosp_tag": "platform-tools-34.0.4", "notes": "I deliberately haven't built this — it's superseded by verified 35.0.2; adb/fastboot are protocol-stable and backward-compatible, so a newer verified version already covers this. I'll revisit only if a specific project requires an exact 34.0.4 pin."}
  },
  "cmake": {"3.22.1": {"status": "shim", "notes": "I create this automatically as a shim delegating to system cmake."}},
  "ndk": {
    "28.2.13676358": {"status": "shim", "tested_on": "ARM64 PRoot Debian real physical Android 16 device", "notes": "This SUPERSEDES the earlier 'do not use' guidance below — I'm keeping that for history, not because it's still accurate. Original finding: direct Flutter/Gradle selection failed at stripDebugDebugSymbols because the bundled linux-x86_64 llvm-strip host executable couldn't start (an environment-specific host-tool execution limitation, not a universal NDK 28/Android ARM64 incompatibility). I fixed this on 2026-09-01 by running `./setup.sh install-ndk 28.2.13676358`, which overwrites the bundled llvm-strip (via the existing llvm-strip->llvm-objcopy symlink, the same structure as my 27.2.12479018 fix) with a shim delegating to my system's real ARM64 llvm-strip. I re-tested this end to end with the project temporarily pinned to this version: `./gradlew clean assembleDebug --no-daemon` succeeded including stripDebugDebugSymbols, the APK installed via ADB, primaryCpuAbi=arm64-v8a, the app launched (PID observed), and a crash/ANR logcat scan was clean. I then reverted the project's canonical pin back to 27.2.12479018 (re-confirmed it still builds clean) — this version is a validated *option* for me, not the new default. `./setup.sh doctor` reports this version's llvm-strip as OK."},
    "27.2.12479018": {"status": "shim", "tested_on": "ARM64 PRoot Debian real physical Android 16 device", "notes": "This is my validated Flutter/Gradle configuration. I pinned the project's ndkVersion=27.2.12479018; a clean assembleDebug succeeds, the APK installs through ADB, Android selects primaryCpuAbi=arm64-v8a, the app launches and survives repeated interaction, and a final crash/ANR scan is clean. The shim delegates llvm-strip to a working compatible path. This is my canonical NDK configuration for the validated PRoot Flutter build path. As of 2026-09-01, `./setup.sh doctor` confirms the bundled llvm-strip resolves to a script shim (not a raw x86_64 ELF) for this version."},
    "27.0.12077973": {"status": "shim", "notes": "I use this as a shim for llvm-strip for older Flutter versions."},
    "26.1.10909125": {"status": "shim", "notes": "I use this as a shim for llvm-strip for older Flutter versions."}
  },
  "cmdline-tools": {"latest": {"status": "verified", "url": "https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip", "notes": "Java-based, runs on any architecture."}},
  "profiles": {
    "validated": {"status": "verified", "components": {"build-tools": "35.0.2", "ndk": "27.2.12479018", "platforms": "android-36"}, "tested_on": "ARM64 PRoot Debian real physical Android 16 device (Redmi Turbo 4 Pro, 25053RT47C)", "notes": "This bundles only already-verified component versions — it doesn't build or verify anything new. It's the exact configuration I validated end-to-end: clean Flutter/Gradle assembleDebug, APK install via ADB, primaryCpuAbi=arm64-v8a, app launch, and a clean crash/ANR scan (see docs/REAL_DEVICE_BUILD_VALIDATION.md). Install with `./setup.sh install-profile validated`."}
  },
  "java-tools": {
    "d8": {"status": "verified", "source": "cmdline-tools/latest/lib/r8.jar", "version_observed": "8.2.33", "execution": "JVM"},
    "apksigner": {"status": "verified-runtime-dependency", "package": "Debian apksigner 35.0.2-1 + libapksig-java 35.0.2-1", "execution": "JVM", "notes": "I used this successfully for controlled APK signing and verification on my ARM64 PRoot Debian environment; I haven't packaged it via ADT itself yet."}
  }
}
