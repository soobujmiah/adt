{
  "build-tools": {
    "37.0.0": {"status": "verified", "aosp_tag": "platform-tools-35.0.2", "tested_on": "Fedora Asahi 43, GCC 15.2.1", "notes": "Uses same AOSP source as 35.0.2 (no platform-tools-37.0.0 tag in AOSP). No release or local artifact is published for this version yet; install = source build."},
    "36.0.0": {"status": "verified", "aosp_tag": "platform-tools-35.0.2", "tested_on": "GitHub Actions ubuntu-24.04-arm (build); ARM64 PRoot Debian real-device fixture (binary execution)", "notes": "No platform-tools-36.0.0 tag exists in AOSP (verified via `git ls-remote --tags https://android.googlesource.com/platform/manifest` on 2026-09-01 — the highest platform-tools-X.Y.Z tag is 35.0.2; Android 16/API 36 is only tagged as android-16.0.0_rN, a full-manifest release tag repos.json's individual component repos are not guaranteed to carry). Uses the same AOSP source as 35.0.2, matching the existing 37.0.0 precedent. Built 2026-09-01 via GitHub Actions (run 33532721357, workflow_dispatch on build.yml); CI's own ARM64 check passed and all 6 build-tools binaries were independently re-verified as ARM64 ELF and actually executed (aapt2, aapt, aidl, zipalign, dexdump, split-select) on the ARM64 PRoot Debian real-device fixture. Added to fill the gap left by build-tools 36.0.0 as distributed by Google's sdkmanager for platforms;android-36, which is x86_64 and cannot execute under ARM64 PRoot (see docs/ANDROID_ARM64_BUILD_HANDOFF.md and the setup.sh doctor host-tool architecture check). Validated offline-installable artifact: artifacts/build-tools-36.0.0-linux-arm64.tar.gz (SHA256 in artifacts/SHA256SUMS). platform-tools 36.0.0 was also built as a side effect of the same AOSP source but is intentionally NOT added here — adb/fastboot are already covered by verified platform-tools 35.0.2 and there was no reason to duplicate that."},
    "35.0.2": {"status": "verified", "aosp_tag": "platform-tools-35.0.2", "tested_on": "Fedora Asahi 43, GCC 15.2.1; ARM64 PRoot Debian real-device fixture", "notes": "Native ARM64 aapt/aapt2/aidl/zipalign toolchain is installed and validated in the ARM64 PRoot environment. D8 8.2.33 JVM path and signed APK verification are validated. Real Android 16 arm64-v8a APK install, JNI loading, native execution, log output, and process survival are validated. Validated offline-installable artifact: artifacts/build-tools-35.0.2-linux-arm64.tar.gz (SHA256 in artifacts/SHA256SUMS)."},
    "35.0.1": {"status": "unverified", "aosp_tag": "platform-tools-35.0.1", "notes": "Should work with base patches, not yet tested"},
    "35.0.0": {"status": "unverified", "aosp_tag": "platform-tools-35.0.0", "notes": "Should work with base patches, not yet tested"},
    "34.0.0": {"status": "unverified", "aosp_tag": "platform-tools-34.0.4", "notes": "Older AOSP, may need additional patches"}
  },
  "platform-tools": {
    "37.0.0": {"status": "verified", "aosp_tag": "platform-tools-35.0.2", "tested_on": "Fedora Asahi 43, GCC 15.2.1", "notes": "Uses same AOSP source as 35.0.2 (no platform-tools-37.0.0 tag in AOSP). No release or local artifact is published for this version yet; install = source build."},
    "35.0.2": {"status": "verified", "aosp_tag": "platform-tools-35.0.2", "tested_on": "Fedora Asahi 43, GCC 15.2.1; ARM64 PRoot Debian real-device fixture", "notes": "adb and fastboot are installed and working. adb was used to install, launch, inspect, and collect logs from the physical Android 16 arm64-v8a device during native APK validation. Validated offline-installable artifact: artifacts/platform-tools-35.0.2-linux-arm64.tar.gz (SHA256 in artifacts/SHA256SUMS)."},
    "35.0.1": {"status": "unverified", "aosp_tag": "platform-tools-35.0.1", "notes": "Should work with base patches, not yet tested"},
    "34.0.4": {"status": "unverified", "aosp_tag": "platform-tools-34.0.4", "notes": "Older AOSP, not yet tested"}
  },
  "cmake": {"3.22.1": {"status": "shim", "notes": "Shim that delegates to system cmake. Created automatically."}},
  "ndk": {
    "28.2.13676358": {"status": "shim", "tested_on": "ARM64 PRoot Debian real physical Android 16 device", "notes": "SUPERSEDES the earlier 'do not use' guidance below — kept for history, not because it is still accurate. Original finding: direct Flutter/Gradle selection failed at stripDebugDebugSymbols because the bundled linux-x86_64 llvm-strip host executable could not be started (environment-specific host-tool execution limitation, not a universal NDK 28/Android ARM64 incompatibility). Fixed 2026-09-01 by running `./setup.sh install-ndk 28.2.13676358`, which overwrites the bundled llvm-strip (via the existing llvm-strip->llvm-objcopy symlink, same structure as 27.2.12479018's fix) with a shim delegating to the system's real ARM64 llvm-strip. Re-tested end to end with the project temporarily pinned to this version: `./gradlew clean assembleDebug --no-daemon` succeeded including stripDebugDebugSymbols, APK installed via ADB, primaryCpuAbi=arm64-v8a, app launched (PID observed), and a crash/ANR logcat scan was clean. The project's canonical pin was then reverted back to 27.2.12479018 (re-confirmed still builds clean) — this version is a validated *option*, not the new default. `./setup.sh doctor` reports this version's llvm-strip as OK."},
    "27.2.12479018": {"status": "shim", "tested_on": "ARM64 PRoot Debian real physical Android 16 device", "notes": "Validated Flutter/Gradle configuration. Project pinned ndkVersion=27.2.12479018; clean assembleDebug succeeds, APK installs through ADB, Android selects primaryCpuAbi=arm64-v8a, app launches and survives repeated interaction, and final crash/ANR scan is clean. Shim delegates llvm-strip to the working compatible path. This is the canonical NDK configuration for the validated PRoot Flutter build path. As of 2026-09-01, `./setup.sh doctor` confirms bundled llvm-strip resolves to a script shim (not a raw x86_64 ELF) for this version."},
    "27.0.12077973": {"status": "shim", "notes": "Shim for llvm-strip. Used by older Flutter versions."},
    "26.1.10909125": {"status": "shim", "notes": "Shim for llvm-strip. Used by older Flutter versions."}
  },
  "cmdline-tools": {"latest": {"status": "verified", "url": "https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip", "notes": "Java-based, runs on any architecture."}},
  "java-tools": {
    "d8": {"status": "verified", "source": "cmdline-tools/latest/lib/r8.jar", "version_observed": "8.2.33", "execution": "JVM"},
    "apksigner": {"status": "verified-runtime-dependency", "package": "Debian apksigner 35.0.2-1 + libapksig-java 35.0.2-1", "execution": "JVM", "notes": "Used successfully for controlled APK signing and verification on ARM64 PRoot Debian; not yet packaged by ADT itself."}
  }
}
